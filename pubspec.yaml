name: flutter_lagos_widget_testing_session
description: 'A Flutter project for Flutter Lagos How To: Widget Testing session.'
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  gap: ^3.0.1
  connectivity_plus: ^6.0.1
  flutter_phosphor_icons: ^0.0.1+6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  plugin_platform_interface:
  connectivity_plus_platform_interface:

flutter:
  uses-material-design: true
  assets:
    - assets/